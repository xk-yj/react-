export interface TabProps {
  /**
   * @description 是否可关闭
   * @default false
   */
  closable?: Boolean;
  /**
   * @description 数据
   * @default tags数组
   */
  tags?: any;
  /**
   * @description 可编辑
   * @default false
   */
  compile?: boolean;
}
